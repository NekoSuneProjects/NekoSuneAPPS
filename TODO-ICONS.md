# Icon / Image TODO (Codex)

Working todo list for icon and image generation work — see [CODEX.md](CODEX.md) for how to use
this file. Separate from [TODO.md](TODO.md), which is Claude's code-level todo list.

Legend: `[x]` done · `[~]` partial · `[ ]` todo

## Sidebar icons (assets/icons/)

All sidebar tabs currently listed in [assets/icons/README.md](assets/icons/README.md) have a
matching icon file. Nothing outstanding as of 2026-07-18.

- [x] Full sidebar icon set generated and matches the table in `assets/icons/README.md`

## Backlog

- [ ] When a new sidebar tab or feature is added, add a row here (and in
  `assets/icons/README.md`) with the tab name + subject prompt as soon as it's known it needs an
  icon, even before the icon itself is generated.

## Notes

- Keep this list truthful: if an icon file already exists in `assets/icons/`, don't list it here
  as outstanding.
- Use the master style prompt in `assets/icons/README.md` for every new icon so the set stays
  visually consistent.
