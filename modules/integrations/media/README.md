# Media integrations

- `soundpadModule.js` controls Leppsoft Soundpad through its local remote-control pipe.
- `photoRelay.js` relays selected local media through the app's supported workflow.

OSC-facing playback and recognition modules live under `../osc/`.
